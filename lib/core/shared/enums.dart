enum Gender { male, female, other }

enum ListingType { business, product }

enum Lookups { division, district, thana, profilePoints, blockReason, flagReason, none }

enum SortBy { recommended, highestRated, mostReviewed, newest, oldest, nameAtoZ, nameZtoA }

enum LeaderboardFilter { monthly, yearly, allTime }

enum WhatsNewType { bug, feature, ui, ux, performance, security }

enum Reaction { dislike, like }

enum AnalyticSource { socialProfile, phone, email, address, website }

enum RatingRange {
  all,
  poor,
  average,
  best,
}

enum DateRangeOption {
  today,
  yesterday,
  thisWeek,
  lastWeek,
  thisMonth,
  lastMonth,
  thisYear,
  lastYear,
  last30days,
  last90days,
  allTime,
  custom,
}

enum SuggestionType { listing, industry, category, subCategory }
